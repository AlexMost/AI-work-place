# NOTES

## Про користувача
- Впевнений TypeScript-розробник (працює з LangGraph/AI-проєктами).
- Мета — фундамент CS, не співбесіда і не разова задача.
- Уже знає: рекурсію, divide-and-conquer через merge sort, базово інші сортування.
- Пише українською → уроки і спілкування українською.

## Підхід до викладання
- Зачіпляти нове за merge sort (відоме) → головний контраст: quicksort робить роботу *до* рекурсії (partition), merge sort *після* (merge).
- Зона розвитку: НЕ вчити рекурсію/Big-O з нуля. Серце нового матеріалу = крок партиціювання.
- Формат: короткі самодостатні HTML-уроки з інтерактивом і миттєвим фідбеком.

## Напрями навчання (наскрізна нумерація уроків/records)
1. **Quicksort** (Mission 1) — фундамент CS.
2. **TypeScript Generics** (Mission 2) — глибше розуміння; уже використовує, своїх не пише.

## Підхід до generics
- Зона розвитку: перехід від споживача generics до автора. Серце уроку 2 = «`<T>` — параметр для типів, який заповнює викликач через inference» + контраст із `any`.
- Далі (коли впевнено пише базовий generic): `extends`-constraints. Поза скоупом поки: mapped/conditional/infer.

## Прогрес
- Урок 0001 — Партиціювання як серце quicksort (Lomuto). Видано.
- Урок 0002 — Твоя перша generic-функція (`<T>` vs `any`, inference). Видано.

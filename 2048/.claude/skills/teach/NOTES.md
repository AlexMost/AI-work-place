# Notes

- **Мова:** українська.
- **Рівень CSS-анімацій:** майже з нуля (знає базовий CSS; transition/@keyframes/transform — туманно).
- **Стиль навчання:** практичний — хоче писати руками, мета «робити сам».
- **Контекст-гачок:** щойно разом зробили гру 2048 (TypeScript + Vite) у цьому ж
  репо (`2048/src/render.ts`, `2048/src/style.css`). Усі приклади прив'язувати до
  реального коду гри — він це вже бачив на власні очі.
- **Сильний гачок для FLIP-уроку:** бачив наживо баг «телепортації» слайду
  (плитка стрибала замість їхати) і фікс через inline `transform` + forced reflow
  (`void layer.offsetHeight`). Будувати майбутній урок саме на цьому досвіді.

## План уроків (чернетка)
1. ✅ Slide: `transition: transform` перетворює стрибок на плавний рух _(0001)_
2. ✅ Easing і тривалість: ease-in-out vs linear, `cubic-bezier`, змінна `--slide` _(0002)_
3. Pop / appear: `@keyframes` + `scale` для появи й злиття плитки (відскок уже намацали в 0002)
4. Пастка телепортації: чому новий елемент не анімується + reflow/FLIP фікс
